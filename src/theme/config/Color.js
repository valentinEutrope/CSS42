import palette from "../defaultColors";

export const primary = palette.blue;
export const secondary = palette.yellow;
export const tertiary = palette.darkenGrey;

export const info = palette.blueSky;

export default {
  primary,
  secondary,
  tertiary,
  info
};
