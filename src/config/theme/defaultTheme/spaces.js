import { lineHeight } from "./variables";

const spaces = {
  none: 0,
  xs: lineHeight / 2,
  sm: lineHeight,
  md: lineHeight * 1.5,
  lg: lineHeight * 2,
  xl: lineHeight * 4
};

export default {
  spaces
};
